apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-metrics.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "app-metrics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "app-metrics.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP